{% extends 'base.html.twig' %}

{% block title %}About index{% endblock %}

{% block body %}
    <h1>About index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>AboutText</th>
                <th>AboutPicture</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for about in abouts %}
            <tr>
                <td>{{ about.id }}</td>
                <td>{{ about.aboutText }}</td>
                <td>{{ about.aboutPicture }}</td>
                <td>
                    <a href="{{ path('app_about_show', {'id': about.id}) }}">show</a>
                    <a href="{{ path('app_about_edit', {'id': about.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_about_new') }}">Create new</a>
{% endblock %}

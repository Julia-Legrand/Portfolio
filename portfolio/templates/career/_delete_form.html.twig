<form method="post" action="{{ path('app_career_delete', {'id': career.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ career.id) }}">
	<button type="submit" class="btn">Supprimer</button>
</form>

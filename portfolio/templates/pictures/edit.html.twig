{% extends 'base.html.twig' %}

{% block title %}Modifier l'image{% endblock %}

{% block body %}
<main class="mainIndex">
    <h2>Modifier</h2>

    <div class="buttons">
        {{ include('pictures/_form.html.twig', {'button_label': 'Valider'}) }}
        {{ include('pictures/_delete_form.html.twig') }}
    </div>
    <a href="{{ path('app_pictures_index') }}" class="btn">Retour</a>
</main>
{% endblock %}

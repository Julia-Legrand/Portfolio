<form method="post" action="{{ path('app_projects_delete', {'id': project.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project.id) }}">
	<button type="submit" class="btn">Supprimer</button>
</form>

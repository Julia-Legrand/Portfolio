{% extends 'base.html.twig' %}

{% block title %}Modifier Réalisations{% endblock %}

{% block body %}
<main class="mainIndex">
    <h2>Modifier</h2>

    <div class="buttons">
        {{ include('projects/_form.html.twig', {'button_label': 'Valider'}) }}
        {{ include('projects/_delete_form.html.twig') }}
    </div>
    <a href="{{ path('app_projects_index') }}" class="btn">Retour</a>
</main>
{% endblock %}
